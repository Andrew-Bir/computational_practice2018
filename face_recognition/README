распознавание/идентификация борна (человека) с помощью библиотек dlib и face_recognition:

В папку known_faces добавляем файлы с изображением людей. 
Имя файла = Имя_Фамилия.jpg

папка test_data в данном случае не обязательная (просто чтобы не потерялись) имя тестового файла может быть любым

запускаем скрипт get-known-faces.py он создает файл arrays.py, в котором сохраняет два массива: первый - с именами известных нам людей, второй - с face_encodings для каждого файла из папки known_faces. Это нужно для того, чтобы при запуске face_identify.py (в котором собсвенно и происходит сравнение face_encodings тестового файла и уже идентифицированных) не перебирать опять весь список файлов известных людей и не делать их кодировку.


